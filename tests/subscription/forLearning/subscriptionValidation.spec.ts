import { test } from '../../../fixtures/forLearning/apiSubscriptions.fixture';

test('Subscription B access validation', async ({ B }) => {
  console.log(`🚀 Running test with Subscription B ID: ${B}`);
  // Your test logic here - Verify from UI
  // Loginin havinng B subscription
  // Verify the feature that B should have 
  // Verify the feature that B shouldn't have 
  // Verify the total number of Students that B can access . 
  // Verify the expiaration Date for B 
});
